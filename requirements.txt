# KINTSUGI Requirements
# Install with: pip install -r requirements.txt
# For full installation with optional dependencies: pip install -e ".[full]"

# Core scientific computing
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0
scikit-image>=0.21.0

# Image processing
opencv-contrib-python-headless>=4.8.0
tifffile>=2023.7.0
imagecodecs>=2023.9.18
imageio>=2.31.0
pillow>=10.0.0

# VALIS registration
valis-wsi>=1.2.0

# libvips bindings
pyvips>=2.2.0

# Visualization
matplotlib>=3.7.0
stackview>=0.18.0

# Data handling
zarr>=2.16.0
h5py>=3.9.0
dask>=2023.7.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
click>=8.1.0
rich>=13.0.0
natsort>=8.4.0
packaging>=23.0

# Additional image processing
basicpy>=1.2.0
aicsimageio>=3.3.1
ome-types>=0.5.0
ome-zarr>=0.9.0
readlif>=0.6.0

# Analysis tools
phenograph>=1.5.0
fast-histogram>=0.14
fastremap>=1.17.0

# Deep learning (optional - uncomment if needed)
# torch>=2.0.0
# torchvision>=0.15.0
# instanseg>=0.0.2
# instanseg-torch>=0.0.9
# kornia>=0.7.0

# Napari visualization (optional - uncomment if needed)
# napari>=0.4.19
# magicgui>=0.7.0

# Java integration (optional - uncomment if needed)
# JPype1>=1.5.0
# scyjava>=1.0.0
# pyimagej>=1.4.0
urllib3>=1.26.19 # not directly required, pinned by Snyk to avoid a vulnerability
tornado>=6.3.2 # not directly required, pinned by Snyk to avoid a vulnerability
aiohttp>=3.9.0 # not directly required, pinned by Snyk to avoid a vulnerability
fonttools>=4.43.0 # not directly required, pinned by Snyk to avoid a vulnerability
setuptools>=65.5.1 # not directly required, pinned by Snyk to avoid a vulnerability
jupyter-core>=5.8.0 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
requests>=2.32.2 # not directly required, pinned by Snyk to avoid a vulnerability
ipython>=8.10.0 # not directly required, pinned by Snyk to avoid a vulnerability
